# This monorepo uses separate env files per app:
#
#   backend/.env.example          → copy to backend/.env
#   backend/frontend/.env.example → copy to backend/frontend/.env.local
#
# See README.md for deployment configuration.
